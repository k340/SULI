#!/usr/bin/env python

"""Will run get_day_fits, group_search, and flag detections if I decide to actually write it"""

# go to 24h_fits
# create session folder
# get_day_fits, put results in (optionally new) subfolder

#